sample_scatter_points:
shift scatterpoint coords with z_middle
      z_to_middle= (image.get_max_index() + image.get_min_index())*voxel_size.z()/2.F;
      scat.coord = org_coord - z_to_middle


integrals:
  scatter_point and detector_coord have to be in same coord system
  shifted_origin -= z_to_middle
  go to image indices via (coord-shifted_origin)/voxel_size

  note: for scatter_point: shift gets undone if image is scat-point-image
